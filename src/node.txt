props: passed into JSX;
state: info about components; changeable; what is usually passed down;
class: extendable object; template for components; hold: key-value pair, functions and properties
constructor: 1st thing that runs; setup method;
OOP: object oriented programming;

git commands:
git clone {address}

npm install;
git init
git add .
git commit -m'(message)'
git push

import things:
1. import (name) from (address)
2. const (name) = require()
